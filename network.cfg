# Edit this file over SSH, then tap WIFI on the MaixCAM screen.
# Values are plain text. Do not add quotes around SSID or password.
ssid=
password=

# Browser: http://MAIXCAM_IP:8080
web_port=8080
stream_port=8000

# 35..95. 75 keeps the 320x240 stream light while preserving detector overlays.
jpeg_quality=75
recordings_dir=./recordings
